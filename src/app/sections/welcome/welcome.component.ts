import { Component } from '@angular/core';

/** Welcome split section plus the full-mode "Kindly Respond" RSVP CTA band. */
@Component({
  selector: 'app-welcome',
  templateUrl: './welcome.component.html',
})
export class WelcomeComponent {}
